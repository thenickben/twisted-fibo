## Fibonacci calculation with an extremely complex architecture

- nginx for routing
- nodeJS/express/postgress API
- redis worker API
- reactJS/axios frontend

### how to run

`docker-compose up --build`

and go to `localhost:3050`
